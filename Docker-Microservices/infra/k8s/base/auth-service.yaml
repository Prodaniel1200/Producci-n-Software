apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: web-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: docker-microservices/auth-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          env:
            - name: USERS_CSV_PATH
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: USERS_CSV_PATH
            - name: GUNICORN_WORKERS
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: GUNICORN_WORKERS
            - name: GUNICORN_THREADS
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: GUNICORN_THREADS
            - name: GUNICORN_TIMEOUT
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: GUNICORN_TIMEOUT
          volumeMounts:
            - name: users-data
              mountPath: /app/data
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
      volumes:
        - name: users-data
          persistentVolumeClaim:
            claimName: users-data-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: web-platform
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 5000
      targetPort: 5000
