apiVersion: apps/v1
kind: Deployment
metadata:
  name: integrations-service
  namespace: web-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: integrations-service
  template:
    metadata:
      labels:
        app: integrations-service
    spec:
      containers:
        - name: integrations-service
          image: docker-microservices/integrations-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          env:
            - name: MS_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: MS_CLIENT_ID
            - name: MS_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: MS_CLIENT_SECRET
            - name: MS_TENANT_ID
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: MS_TENANT_ID
            - name: GUNICORN_WORKERS
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: GUNICORN_WORKERS
            - name: GUNICORN_THREADS
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: GUNICORN_THREADS
            - name: GUNICORN_TIMEOUT
              valueFrom:
                configMapKeyRef:
                  name: platform-config
                  key: GUNICORN_TIMEOUT
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: integrations-service
  namespace: web-platform
spec:
  selector:
    app: integrations-service
  ports:
    - name: http
      port: 5000
      targetPort: 5000
